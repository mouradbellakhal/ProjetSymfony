{% extends 'base.html.twig' %}

{% block header %}<h1>Login</h1>{% endblock %}

{% block body %}
<div>
    {% if error %}
        <div class="alert alert-danger">
            {{ error.messageKey|trans(error.messageData, 'security') }}
        </div>
    {% endif %}

    {{ include('/user/_login.html.twig', {'last_username': last_username}, false) }}
</div>
{% endblock %}
